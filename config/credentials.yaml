# config/credentials.example.yaml
# Copy to credentials.yaml and fill in your details
# credentials.yaml is git-ignored

physionet:
  username: "bourbonsour"
  password: "jaiguruji15"

# Optional: BigQuery for MIMIC-IV clinical data
bigquery:
  project_id: "your-gcp-project"
  credentials_file: "path/to/service-account.json"

# Optional: Local PostgreSQL with MIMIC-IV loaded
postgres:
  host: "localhost"
  port: 5432
  database: "mimic4"
  user: "mimic"
  password: "your_db_password"